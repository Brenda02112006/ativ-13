<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Buscador de Livros</title>
  <!-- Adicionando Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
  <style>
    /* CSS para centralizar o conteúdo */
    body {
      display: flex;
      flex-direction: column;
      align-items: center;
      background-color: rgb(158, 167, 241); /* Fundo vermelho */
      margin: 0; /* Removendo margem padrão do body */
    }
    /* Estilo adicional para o quadrado */
    .container {
      margin-top: 1px; /* Espaçamento superior */
      box-sizing: border-box; /* Incluir borda e padding na largura total */
      display: flex;
      flex-direction: column;
    }
    /* Estilo adicional para o botão de busca */
    .btn-buscar {
      margin-top: 10px; 
    }
    /* Estilo adicional para a listagem de livros */
    .book-list {
      margin-top: 20px;
      padding: 10px;
      background-color: #fff;
      border-radius: 5px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }
  </style>
</head>
<body>

  <h1 class="text-center">Buscador de Livros</h1>
  <div class="container">
    <form action="/search" method="GET">
      <div class="form-group">
        <label for="title">Buscar por Título:</label>
        <div class="input-group">
          <input type="text" class="form-control" id="title" name="title">
          <div class="input-group-append">
            <div class="input-group-text">
              <input type="checkbox" id="titleCheck" name="titleCheck">
            </div>
          </div>
        </div>
      </div>
      <div class="form-group">
        <label for="year">Buscar por Ano:</label>
        <div class="input-group">
          <input type="text" class="form-control" id="year" name="year">
          <div class="input-group-append">
            <div class="input-group-text">
              <input type="checkbox" id="yearCheck" name="yearCheck">
            </div>
          </div>
        </div>
      </div>
      <button type="submit" class="btn btn-primary btn-buscar">Buscar</button>
    </form>

    <% if (books && books.length > 0) { %>
      <div class="book-list">
        <h3>Resultados da Busca:</h3>
        <ul>
          <% books.forEach(book => { %>
            <li><%= book.title %> - <%= book.author %> (<%= book.year %>)</li>
          <% }) %>
        </ul>
      </div>
    <% } else if (books && books.length === 0) { %>
      <p class="book-list">Nenhum livro encontrado.</p>
    <% } %>
  </div>
</body>
</html>
